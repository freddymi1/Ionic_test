import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'landing_page',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
